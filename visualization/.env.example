# Main atlas only; the local /dataset route needs no token or CDN.
VITE_MAPBOX_TOKEN=
VITE_CDN_URL=
VITE_LOCATIONS_URL=
# Optional analytics; disabled for local /dataset uploads.
VITE_POSTHOG_KEY=
VITE_POSTHOG_HOST=
