# Tested with Python 3.11 and the CUDA 12.4 PyTorch wheels.
# Install torch/torchvision from the CUDA index first (see README).
torch==2.6.0
torchvision==0.21.0
transformers==4.51.3
accelerate==1.6.0
timm==1.0.15
